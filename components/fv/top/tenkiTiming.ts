/**
 * 転記（てんき）— トップ FV の時間軸（秒）
 *
 * ステージ時計 t=0 ＝「OP の筆致がほどけて断片になった瞬間（OpeningLite の onDone）」。
 * OP のあいだ（約 1.2s）にステージは版下の焼き付け・断片の生成・流体の用意を
 * 済ませてあるので、渡された瞬間から待ちなく走り出す。
 *
 *   0.00  散らばり  事務のデータの断片が漂う（＝バラバラな事務作業）
 *   0.55  整列      回転が戻って水平に揃う。揃う瞬間に小さく行き過ぎて戻る（カチッ）
 *   1.05  連結      隣と端が橋で繋がり、繋がった線を左→右へ光が走る
 *                   （＝繋がっていないものを繋ぐ、の核）
 *   1.35  一本化    帯が 1 本の細い線に潰れる
 *   1.55  筆        線が筆になって題字を書く。書かれた側から DOM の文字が現れ、
 *                   版下は同じだけ薄れて消える（題字の上には何も残らない）
 *   2.00  定着      墨の一滴・灯・流体
 *
 * 末尾の文字が読める時刻 ≒ writeStart + writeDur + letterFade = 2.12s
 * （OP 込みでページのマウントから約 3.3s。初速より見せ場を優先する設計）
 *
 * 版下が writeStart に間に合わなければ、その時刻で時計を止めて「一本化した線」の
 * まま待つ（holdMax が上限。超えたら筆を省いて題字を出し、定着へ進む）。
 */
export const TENKI_T = {
  /** ステージの不透明度が立ち上がる時間 */
  fadeIn: 0.28,
  /** 整列の開始・所要 */
  alignStart: 0.55,
  alignDur: 0.5,
  /** 連結（橋＋光の走り）の開始・所要 */
  connectStart: 1.05,
  connectDur: 0.3,
  /** 一本化の開始・所要 */
  collapseStart: 1.35,
  collapseDur: 0.2,
  /** 筆の掃引の開始・所要（3 行ぶんの合計） */
  writeStart: 1.55,
  writeDur: 0.4,
  /** 掃引時間のうち「行から行への返し」に充てる割合＝字画の切れ目で筆が持ち上がる */
  returnShare: 0.16,
  /** DOM の文字がクロスフェードする時間（版下が薄れる時間でもある） */
  letterFade: 0.17,
  /** 書き終えたあと筆先が消えるまで */
  brushOut: 0.14,
  /** 墨の一滴が落ちる時間（settle の手前） */
  dropFall: 0.2,
  /** 定着＝灯がともり、墨が滲み、他要素が入場する */
  settle: 2.0,
  /** 版下が間に合わないとき、一本化した線のまま待てる上限（秒） */
  holdMax: 1.2,
  /** 何があっても DOM の題字を出す壁時計の上限（rAF 停止などの最終保険） */
  hardDeadline: 3.2,
} as const;

/** 軽量経路（タッチ・狭幅・reduced-motion）＝静止 1 コマ側の刻み */
export const TENKI_STILL = {
  letterDelay: 0.15,
  letterStagger: 0.03,
  letterDur: 0.55,
  /** 他要素の入場を始める時刻 */
  settle: 0.85,
} as const;
